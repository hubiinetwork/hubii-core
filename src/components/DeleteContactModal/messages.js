/*
 * DeleteContactModal Messages
 *
 * This contains all the text for the DeleteContactModal component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.DeleteContactModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeleteContactModal component !',
  },
});
